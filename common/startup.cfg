#GCODE to run at klipper startup
[delayed_gcode push_config]
initial_duration: 30 #was 5 — allows Moonraker webhooks to register before shell output
gcode:
    RUN_SHELL_COMMAND CMD=github_backup
    STATUS_OFF