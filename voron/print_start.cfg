[gcode_macro PRINT_START]
gcode:
    {% set X_MAX = printer.toolhead.axis_maximum.x|default(100)|float %}
    {% set Y_MAX = printer.toolhead.axis_maximum.y|default(100)|float %}

    {% set BED_TEMP = params.BED|default(60)|float %}
    {% set EXTRUDER_TEMP = params.EXTRUDER|default(190)|float %}

    STATUS_BUSY
    G90 #set absolute positioning
    M107 #disable part cooling fan
    
    SET_HEATER_TEMPERATURE HEATER=heater_bed TARGET={BED_TEMP} #heat up bed
    SET_HEATER_TEMPERATURE HEATER=extruder TARGET=150 #hold extruder at 150

    STATUS_HOMING
    M117 Homing
    M118 Homing...
    #G28 #home all axis
    HOME_IF_NEEDED
    G0 X{X_MAX/2} Y{Y_MAX/2} Z50 F3600 #move extruder 50mm above bed centre

    M117 Heating bed
    M118 Heating bed...
    STATUS_HEATING
    TEMPERATURE_WAIT SENSOR=heater_bed MINIMUM={BED_TEMP} #wait for bed to hit target temp

    {% if printer.quad_gantry_level.applied and printer.z_calibration.last_z_offset %}
        M118 QGL already applied
    {% else %}
        M117 Attaching probe
        M118 Attaching probe...
        Attach_Probe_Lock #prevent probe docking until unlocked, from klicky    

        M117 Quad Gantry Levelling
        M118 Quad Gantry Levelling...
        STATUS_LEVELING
        QUAD_GANTRY_LEVEL_KEEP_PROBE #V2.4

        M117 Re-homing Z
        M118 Re-homing Z
        STATUS_HOMING
        G28 Z #rehome Z axis

        M117 Z Calibration
        M118 Z Calibration...
        STATUS_CALIBRATING_Z
        CALIBRATE_Z #automatic Z offset, from klipper z calibration        
    {% endif %}
    
    #M117 Cleaning nozzle
    #CLEAN_NOZZLE #requires brush/purge bucket, from decontaminator
    
    M117 Meshing bed
    M118 Meshing bed...
    STATUS_MESHING
    BED_MESH_PROFILE LOAD=default
    #BED_MESH_CALIBRATE #generate new mesh

    SET_HEATER_TEMPERATURE HEATER=extruder TARGET={EXTRUDER_TEMP}
    
    M117 Moving
    M118 Moving...
    STATUS_BUSY
    Dock_Probe_Unlock #removes probe lock
    G0 X175 Y175 Z50 F3600 #move extruder 50mm above bed centre

    M117 Heating extruder
    M116 Heating extruder...
    STATUS_HEATING
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM={EXTRUDER_TEMP}

    M83 #Relative extruder mode
    G92 E0 #Reset extruder to 0

    M117 Printing
    M118 Printing
    STATUS_PRINTING